#ifndef TOPOLOGY_TYPES
#define TOPOLOGY_TYPES

#include <cmath>
#include <stdio.h>
#include <vector>
#include <functional>
#include <eigen3/Eigen/Dense>
#include "PanicButton.h"

using namespace Eigen;

enum TopologyType {TPoint, TLine, TTriangle, TQuadrilateral, TTetrahedron, THexahedron};

#endif

